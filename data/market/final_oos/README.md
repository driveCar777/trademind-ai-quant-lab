Final OOS isolation.

`LOCK.json` starts with `FINAL_OOS_LOCKED = false`.

Do not put “latest download” here. Do not lock a window in V0.1.
Once locked, parameter changes against that window are refused.
