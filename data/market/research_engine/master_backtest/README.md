# V9 Master Backtest

Unified replay of already-defined mechanisms on already-owned data.

- `TRADE_MIND_MASTER_BACKTEST.csv` — one research-window row per book
- `MASTER_METRICS.json` — counts, layers, STOP
- `ledgers/` — `equity.csv`, `trades.csv`, `metrics.json`
- `curves/` — best-of-layer copies, or `NO_POSITIVE_EQUITY_CURVE`

No purchase. Final OOS denied. HYP-0001 is predictive-only.
