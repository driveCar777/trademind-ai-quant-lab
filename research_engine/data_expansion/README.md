# Data Expansion

Machine-readable factory for Information Gap → vendor research → purchase priority → acquire → qualify → unlock.

Canonical JSON lives under `data/market/research_engine/data_expansion/`.
This package only loads and gates. It does not invent consensus, smiles, or futures curves.
