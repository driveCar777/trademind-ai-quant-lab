# Factor Discovery V0.1

Windows owns the contract. Xavier only executes `node_eval.py`.

Remote dir: `/tmp/tm-factor-discovery-v01` (not HYP-0001 `/tmp/tm-research-engine-v04`).

Dispatcher: `scripts/research_engine_factor_run.py`.
