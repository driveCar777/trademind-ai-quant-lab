# DIFF 2026-08-23 — Xavier-01 纳入四台启动

不是 V4。不改 Worker 计算代码。不停 01 做破坏性重启。

## 澄清

4 台都在当前阶段。以前一键跳过 01，只因为 Docker 且当时已在线。

## 行为

- `start_xavier.bat` / `start_lab.bat`：01–04 同等，已健康跳过。
- 01：`docker start` / `docker restart`，只认固定箱名。
- 网页 `POST .../worker-01/start` 同一脚本。

## 冒烟

`tests/smoke/08_xavier01.py` → `SMOKE_08_PASS`
