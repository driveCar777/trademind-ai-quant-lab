# V35 判决 — `CN_INFO_TO_COMMODITY_V35_NO_CANDIDATE`

> 2026-09-06 10:25。合同先提交后跑，只算一次。指数日线冻结到 **2024-02-29**（与 A 股面板同一截断），验证期实际只有 8 个持有期，不是 36。ATLAS 79。

## 1. 闸门

| 门 | 结果 | 读数 |
|---|---|---|
| G1 验证 LONGFLAT > 0 | 过 | +11.9%（8 期） |
| G2 t ≥ 2 | **不过** | t **1.86** |
| G3 滚动 4/5 | 过 | 4/5（2019–20 整段空仓 = 0） |
| G4 研究 > 0 | 过 | +18.4%（CAGR 4.2%，t 0.83） |
| G5 打过 EW4 | **不过** | 验证期 **永远做多**（frac_on=1），LONGFLAT ≡ EW4 |

**3/5。** 标签 `NO_CANDIDATE`。

## 2. 读法

- 模型学到的预测值在 −0.5%～+0.5% 之间，阈值 0 在验证短窗里从未翻面：没有择时，只是「这 8 期商品篮子本身在涨」。
- 研究期有择时（51% 时间在场），但 2019–2020 整段空仓，错过的 EW4 让研究 LONGFLAT（+18%）**弱于**死拿（+36%）。择时在有样本的地方是负贡献。
- 指数文件止于 2024-02-29，2024-03 之后无法打分。不把「去更新指数再读一次验证」当成下一刀——那是用同一合同改数据窗。

## 3. 禁止与归位

不调阈值、不加 V31 特征、不扩品种、不把 8 期 +12% 当 Candidate。`CN_INFO_TO_COMMODITY` 这一格划掉。

至此，对照失败图谱：MT5 D1 合法新对象 = 0，国内期货价格层 = 0，A 股→商品映射 = 0。**组合仍是 1 袖（ML1 / V26.8）**。接下来不是再发明一个家族，而是让 V29 影子账本按 V26.8 往前记；人的模拟盘用每天的 SHORTLIST。
