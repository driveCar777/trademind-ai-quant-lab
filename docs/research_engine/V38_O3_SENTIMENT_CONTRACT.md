# V38-S2 O3 合同 — 市场级散户情绪（"韭菜指数"）减仓叠加（写于跑之前，2026-09-07 01:05）

> 代码 `research_engine/v38_overlay/sentiment.py`。作用对象 = 冻结的 V26.8 研究/验证账本（与 O1/O2 相同）。**只读一次**，第二次拒绝。不改 ML1、不改名单。

## 用户的问题

"有没有韭菜指数、散户存在指数、羊群、心理之类的？" → 有四个免费可算的代理，合成一个市场级过热指标，只用来决定**仓位 100% 还是 50%**。

## 四个分量（每个在信号日 sd 用 ≤ sd 的数据算 250 日滚动 z 分）

| 分量 | 定义 | 来源 | PIT |
|---|---|---|---|
| MARGIN_Z | log(全市场融资余额合计) 的 20 日变化 | V23 `RZYE.npy`（已含 D+1 滞后） | 已 PIT |
| TURN_Z | 合格名字换手率中位数的 20 日均值 | pack `turn` | 收盘可得 |
| LIMITUP_Z | 合格名字里收盘涨幅 ≥ 9.5% 占比的 20 日均值 | pack `close/preclose` | 收盘可得 |
| NEWACCT_Z | 中登月度新增投资者（万户），36 月滚动 z | 东财 `RPT_STOCK_OPEN_DATA`（2015-04 → 2023-08） | 统计月次月 15 日后可见 |

合成 = 可用分量 z 的算术平均（NEWACCT 缺失时用 3 个）。

## 唯一规则

`O3_SENT`：sd 时合成 z **> +1.0**（过热）→ 该期仓位 0.5，否则 1.0。阈值、窗口、分量事前固定；不搜。

## 判决门（与 O1/O2 相同）

验证期：叠加后逐日 MaxDD 改善 **且** 夏普不降 → `ADOPT_CANDIDATE`；否则 `REJECT`。研究期只报。不读禁用窗。

## 禁止

不试 +0.5 / +1.5；不换 0.5 仓位比例；不拆分量单独测；不做多空方向（低情绪加仓 — 用户账户无杠杆）。
