# Ava GOLD 手工跟盘规格（不是 Candidate，不是实盘合同）

> **LEGACY / FROZEN (Phase 2, 2026-09-13).** Display-only V4 beta. Not a research Candidate.  
> Do **not** retune 252/20 or write this into Grok / order_send.  
> 2026-09-13。主人目标：在 Ava MT5 黄金上尽量用**已经量过的边**试着赚钱。  
> 本文件是给人看的跟盘说明书。`candidate=false`，`deploy=false`，不 `order_send`，不写 Grok。

## 能不能诚实地说「有一条已经量过的路」？

**能，但只有一本账，而且不是小时波段。**

本机唯一过历史闸门的 Ava 黄金账本是 **D1 十二个月 TSMOM V4**（`VIABLE_HISTORICAL`，验证 +84% t 2.18，全样本 MaxDD −47%），以及同一符号的 **V5 波动倒数外壳**（仍是历史过门，MaxDD −40%）。研究窗为负。全样本不是承诺。10%/月不是闸门（黄金自己也很少有 +10% 的月份）。

own-price H1 树 / 场次 / 障碍 / 稀疏五列已清点并停止（`STOP_GOLD_H1_OWN_PRICE_ML_AND_RULES`）。不要再训 24h 符号树，不要把 H1 规则抄到 M15。

## 产品

| 项 | 值 |
|----|----|
| 经纪商品种 | Ava **`GOLD`**（`GOLD_META.json`：`broker=GOLD`，点 0.01，合约 100） |
| 逻辑名 | `XAUUSD`（:9001 热台映射；终端列表里常见是 `GOLD` 不是 `XAUUSD`） |
| 周期 | **D1**。信号按日线算，人按下一根日线开盘跟 |
| 仓位 | 默认一笔；可选 V5 手数上限，**不加杠杆** |

## 规则（与冻结 V4 相同）

1. `mom = close[t] / close[t−252] − 1`（不够 252 根 = 空仓）
2. `mom > 0` → 多；`mom < 0` → 空；`mom = 0` → 空仓
3. **下一根 D1 开盘**进；持有 **20** 根 D1；非重叠
4. 成本已在历史账本里（点差 + 2×2bp + swap）。人跟盘仍会付真实点差和隔夜
5. 可选 V5 仓位：`w = min(1, 0.10 / σ20d_ann)`，只缩小手数，不放大

## 人在 MT5 上怎么做

- **同一时间只持一笔 GOLD。**
- 只在**新的信号日**翻仓：当前 20 根走完，再看新的 252 日符号；符号没变就同向再开，变了才反手。
- **不要用这个信号做小时/15 分钟剥头皮。** 量到的边是多月趋势。
- 9 月这种小时砸盘**不构成翻空理由**——只要 252 日动量仍为正，规则继续做多。
- 手数：小账户用经纪商最小手（常见 0.01）。若用 V5，把最小手再乘 `w`（当前信号日约 0.37，约等于 0.01 手再打四折；打不了折就仍用 0.01，不要加杠杆凑满仓）。
- 本页 / `STATUS.json` 只告诉你现在该多还是空。不代替你点下单。

## 当前隐含仓位（最后一根 D1 = 2026-09-11）

| 项 | 值 |
|----|----|
| V4 / 最后一根日线符号 | **多**（`close` 4348.78 / 252 日前 4064.71，动量 **+7.0%**） |
| 未平这腿 | 信号 **2026-08-24**，进场 **2026-08-25** 开盘 **4679.82**，20 根未到期 |
| 日线盯市（进场开盘→最后收盘） | **约 −7.1%** |
| 日线盯市（进场开盘→最后开盘） | **约 −7.8%** |
| 已诊断的 9 月小时砸盘 | 2026-08-11→09-11 死拿多 −1.2%，区间最大回撤 **−8.4%**（最深 9 月 2 日 01:00，4291）；未平腿小时盯市 **−7.1%** |
| V5 信号日仓位 | **0.37**（最后一根约 0.44） |
| 预计出场 | 已持有 15 根，还剩 **5** 根 D1；`planned_exit_est` **2026-09-18**（按工作日外推。黄金 D1 含额外场次时可能更早。旧诊断「约 9 月 22 日」是按 20 个工作日从进场硬加，偏晚） |

规则不会因为 9 月砸盘翻空。252 日动量仍是正的。

## 风险（数字，不是口号）

- 全样本 MaxDD：**V4 −47% / V5 −40%**
- 102 笔里 **81 多 / 21 空**；空头每笔净约 **−1.4%**（空头袖子是漏）
- 研究窗（前 70%）**−10%**；验证窗 **+84%** 主要吃的是 2024–26 金牛，不是稳定的低命中高盈亏比
- 年切片：2021 −18%、2022 −23%、2024 +37%、2025 +59%、2026 已结算 **−4.3%**
- 命中 54%、盈亏比 1.15 — 仍不是 CTA 教科书
- **不是每月 10% 的机器。** 黄金自身 CAGR 约 17.5%，极少单月 +10%

## 禁止

再训 H1 树；按 IC 挑特征；把 H1 规则抄到 M15；加大 k；把分数写进 Grok；`order_send`；改 `:9000` / `daily.py` / `paper_ops.py` / `live/paper/JOURNAL.json`；改 252 / 20 / 10%；把本规格升格成 Candidate；看见路径诊断后把保本 / 移动 / 止盈写进本规格（`HOT_MT5_GOLD_V4_PATH_EXITS.md` 是诊断，不是新账本）。
