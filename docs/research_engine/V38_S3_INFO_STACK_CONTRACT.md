# V38-S3 合同（预登记，**未跑，也不能在现有窗口上跑**）— 全信息层堆叠 ML7

> 2026-09-07 02:00 写。这是 S1 三层同墙之后唯一合规的下一步；它需要一个没被任何人看过的验证窗，而所有历史窗都已消耗。因此本合同只**登记**特征、模型、闸门与读取条件，等 V29 前向数据积累到位再执行。写它的目的：把"以后用什么、怎么读"在看到任何前向结果之前钉死。

## 为什么不在旧窗上跑

- 研究期 2010–2021、验证期 2021-08→2024-02、禁用窗 2024-03→2026-08（V28 已读）全部被 ML1 / ML2F / L1 / L4 / L7 各自看过。
- 决定"哪些层进堆叠"的信息来自这些层的验证结果 → 在旧窗上再跑 = 用验证期选特征。**禁止。**
- 唯一干净的数据：V29 每个交易日新生成的 live pack（`ml1_live/daily.py`）。它从 2026-08-28 起走，未被任何模型看过。

## 特征（32 个，全部已 PIT 编译；不含任何价格特征）

| 层 | 数量 | 代码 | 单层验证超额 t（已看过，仅作记录） |
|---|---|---|---|
| 季报 V27 ML2F | 10 | `cn_a_share_findeep_v27` | 5.6 |
| 业绩预告 / 快报 L1 | 9 | `cn_a_share_preann_v38` | −0.1 |
| 股东 / 高管增减持 L4 | 7 | `cn_a_share_insider_v38` | 5.9 |
| 股权质押 L7 | 6 | `cn_a_share_pledge_v38` | 6.6 |

**全部纳入**（包括 L1）——纳入规则是"S1 已编译的信息层全进"，不是按结果挑。

## 模型

与 V25 完全相同的 LightGBM 参数与 walk-forward；训练用 2010→2024-02（研究+验证，因为这两段对堆叠而言只是训练数据，不再作为评估）；**禁用窗 2024-03→2026-08 也只作训练**（已被 ML1 读过，不能再当评估）。首个评估预测 = `daily.py` 前向日期 ≥ 2026-08-28。

## 账本与门（用户执行现实）

- 闸门账本 = **LO20 多头**（与 A1 相同；用户没有对冲工具）。
- 用 V29 前向：`SIGNAL_{date}.json` 同时输出 ML7 分数（不进 SHORTLIST，只记影子账本 `LEDGER_ML7.json`）。
- **读取条件**：前向 ≥ **24 个不重叠 20 日期**（≈2 年）后读一次；门：超额 vs EW > 0 且 t ≥ 2.0、LO20 资金 > 0、与 ML1 前向超额 corr < 0.90。
- 通过 → ML1 + ML7 等权两袖（S3）；未通过 → 关闭，不调。

## 现在做什么（2026-09-07 10:20 已落地，用户 09:44 授权）

1. `research_engine/ml7_live/`（见其 README）：四层增量刷新到 `live/info_layers/`、32 特征、REFIT_240 同表模型（2025-11-06，训练行 2,446,408）、`SIGNAL_ML7_{date}.json` + `LEDGER_ML7.json`；`ml1_live/daily.py` 第 9 步在 ML1 全部文件写完后调用，`--no-ml7` 可关，异常只记录。
2. 首个前向信号 2026-08-28：999 只，与 ML1 前 20% 重合 **38.6%**（名单确实不同）。最近 refit 增益前列 BP 18%、PL_NEG_MCAP 10%、PL_NEG_RATIO 9%。
3. `LEDGER_ML7.json` 状态 `ACCUMULATING`，`periods_until_read` = 24。**读取前任何数字都不是结论。**

## 禁止

在任何历史窗口上评估本堆叠；按单层结果挑层；调参数；提前读。
