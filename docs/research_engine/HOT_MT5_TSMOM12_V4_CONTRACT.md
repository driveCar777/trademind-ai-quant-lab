# HOT_MT5_TSMOM12_V4 合同（跑前冻结）

> 2026-09-13。长任务队列第 3 步。回答用户要的「命中低、盈亏比高」教科书形态。
> 无树。不是 V1/V2/V3 补丁。不是 CME 截面 FX3（那是 30 只期货 XS）。禁止改成 SMA200。

## 信号（成交前写死）

`mom[t] = close[t] / close[t-252] − 1`（不够 252 根 = 空仓）

- `mom > 0` → LONG
- `mom < 0` → SHORT
- `mom = 0` 或不足历史 → CASH

无阈值、无 ATR、无 ML、无 class_weight。下一根开盘进，持有 **20** 根 D1（月频再平衡的交易日近似），非重叠，0.01 手只记账。成本 = 点差 + 2×2bp + 实际隔夜（与 V1 同一公式）。

七个品种各算一条，同一规则。美股不做。不重拉。

## 闸门

主报告 = 全样本。验证 = 后 30%。验证成交 &lt; 8 或覆盖 &lt; 15% → `NO_CANDIDATE`。否则验证 TWR&gt;0 且 t&gt;1 → `VIABLE_HISTORICAL`，`candidate` 仍 false。

## 禁止

改 252 / 20；加均线过滤；波动倒数仓位（那是另一份，不在本份）；看完某年再切品种；写 Grok；覆盖 V1–V3 READ；A 股对冲。
