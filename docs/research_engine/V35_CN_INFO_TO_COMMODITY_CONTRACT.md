# V35 合同 — A 股信息 → 国内工业金属/能源（`CN_INFO_TO_COMMODITY_V35`）

> 2026-09-06 10:20，跑前冻结，只算一次。V31 之后清单上唯一事先点名的合法新层（`MT5_D1_LEGAL_OBJECT_INVENTORY.md`）。不重开 V31 的 8 个价格/持仓/期限结构特征。$0。m+1。

## 1. 对象

- 品种（事前固定，MT5 可映射）：**CU、AG、AU、SC**。
- 收益 = V31 已冻结的同合约 20 日开盘→开盘（`fwd_same`），不重算连续价差。
- 信息层 = **A 股已有指数 PIT**（`index/daily/HS300.csv`、`ZZ500.csv`），收盘后可知。不用 V31 的 8 特征，不用行业成员因子（V16 已证伪），不采购。

## 2. 特征（5 个，全市场、四个品种当日相同）

`HS300_RET_20`、`ZZ500_RET_20`、`SIZE_SPREAD_20`（ZZ500−HS300）、`HS300_REV_5`、`HS300_VOL_60`（HS300 日收益 60 日标准差，取负号：低波动为正）。全部 ≤ t。

因为 X 在截面上相同，模型学的是「A 股状态 → 工业金属/能源篮子的期望收益」，不是品种排序。

## 3. 模型

- 一行 = (日期, 品种)。LightGBM 回归，V25 `LGBM_PARAMS` 逐字。目标 = 该品种 20 日同合约收益（不排名，因为截面无差异）。
- 训练 2018-06-01 起、stride 5、embargo 21。首预测 **2019-07-01**，研究期每 240 日 refit，**2023-06-30 冻结**。划分同 V31。

## 4. 账本

- **闸门 = 篮子择时 LONGFLAT**：当日预测值 > 0 则等权做多四个有标签的品种，持有 20 日；否则空仓。成本 3.3 bp/边（同 V31）。
- 诊断：始终满仓等权四品种（EW4），看择时有没有比「死拿」强。

## 5. 门

- G1 验证 LONGFLAT 资金 > 0
- G2 验证期均 t ≥ **2**（事前降低：4 品种 × ~36 期，功效低于 V31；不是事后放宽）
- G3 滚动 5 块（同 V31）≥ 4/5
- G4 研究 LONGFLAT > 0
- G5 验证期收益 vs EW4 > 0（择时必须打过死拿）

全过 → `CN_INFO_TO_COMMODITY_V35_LEVEL1`；否则 `NO_CANDIDATE`。

## 6. 事前预期

弱。A 股指数与国内商品的相关不稳定；V17/V32 同类宏观映射已失败。本层是把「中国信息→商品」这一格划掉，不是高期望 alpha。

## 7. 禁止

不把 V31 特征加回来；不扩品种名单；不改阈值 0；不用验证期挑多空；不映射 MT5 除非 LEVEL1。
