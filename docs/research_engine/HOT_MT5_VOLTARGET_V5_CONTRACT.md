# HOT_MT5_VOLTARGET_V5 合同（跑前冻结）

> 2026-09-13。原设计 `HOT_MT5_SITOUT_AND_HURDLE_DESIGN.md` 写明：12 月 TSMOM 与**波动倒数仓位**是两份互不替代的对照。V4 合同也写了「波动倒数是另一份」。
> 这不是看完黄金 MaxDD −47% 再加的补丁。数字来自 Barroso–Santa-Clara / 常见 CTA 实现，七个品种同一套。
> 不改 V4 的 252 / 20。无树。不是 SMA200。`candidate=false`。

## 信号

与 V4 相同：`mom = close[t]/close[t-252]−1` → 符号做多/空。不够 252 根空仓。

## 仓位（成交前写死）

`σ_ann[t] = std(日收益[t-19 … t]) × √252`（只用 t 及以前）

`w[t] = min(1.0, 0.10 / max(σ_ann[t], 1e-8))`

- 目标年化波动 **10%**：Barroso–Santa-Clara 常值波动缩放的常用目标，不是黄金回撤反推。
- **上限 1.0，不加杠杆**（小资金纸面，0.01 手只记账）。
- 波动窗口 **20** 日 = 与持有期同一数量级，事先对齐，不搜 10/60。
- 净收益 = `w × (毛 − 成本)`，成本随仓位缩放。

下一开盘、持有 20 根、非重叠、同一成本公式。

## 闸门

与 V4 相同。验证成交 &lt; 8 或覆盖 &lt; 15% → 否。否则验证 TWR&gt;0 且 t&gt;1 → `VIABLE_HISTORICAL`，`candidate` 仍 false。

## 禁止

改 252 / 20 / 10% / 封顶；只保留黄金；加 SMA；覆盖 V1–V4 READ；写 Grok；A 股对冲。
