# 黄金 H1 树：有没有在自己的训练集上测过

> 2026-09-13。只读诊断。不改 V1–V5 `READ.json`。机器文件 `gold_h1_v1/results/TRAIN_VAL_REGIME.json`。成交再对账 1752 笔，错位 0。不是 Candidate。

主人要的是：**先证明模型在自己的训练行上能不能拟合，再谈样本外，再谈状态。**  
此前 `research_70 = −46%` 是 walk-forward 样本外，不是「训练集拟合质量」。那一刀没做。这一刀做了。

## 1. 真样本内（研究窗同一批行：先拟合，再预测这些行）

同一套 `LGBM_PARAMS`（200 棵、15 叶、min_child 40）。研究窗 = 后 30% 验证切点之前、特征与 24h 标签都有限的行。

| | 行数 | IC | R² | 符号命中 | 分数 min / max / std | 不同分数个数 | 卡死常数 |
|--|------|----|----|----------|----------------------|--------------|----------|
| **H1_ML（V1 十四列）** | 32,473 | **0.517** | **0.187** | **62.2%** | −2.9% / +4.0% / 0.22% | 30,332 | 否 |
| **H1_NATIVE（V5 小时钟）** | 32,553 | **0.485** | **0.178** | **63.4%** | −3.4% / +5.0% / 0.23% | 30,718 | 否 |

日线黄金那次「分数塌成两个常数」**这里没有**。树能拟合自己的标签。特征/标签不是死的。不是「再加 epoch」。

研究窗单列对 24h 标签的 Pearson 全在 |IC| < 0.04（最大 `VOL20` −0.033）。单列几乎没边；树靠组合在样本内抠出 IC 0.52。

## 2. 44 折：净化训练集 IC vs 下一折样本外 IC

和 V1 同一套：首预测 2000、每 1000 重拟合、embargo=25。每一折：在训练行上预测（真样本内），再预测随后 1000 根（OOS）。

**H1_ML**

| | 训练 IC | 测试 IC | 训练命中 | 测试命中 |
|--|---------|---------|----------|----------|
| 折均 | **0.619** | **0.038** | — | — |
| 折中位 | 0.574 | 0.065 | — | — |
| 最小 / 最大 | 0.485 / 0.907 | −0.316 / 0.372 | — | — |
| 测试 IC>0 | — | 23/44 | — | — |

首折（训练 1776 行）：训练 IC 0.91、R² 0.76、命中 87%；测试 IC 0.15、R² −0.22、命中 55%。  
末折（训练 44776 行）：训练 IC 0.51；测试 IC 0.09。  
全程 walk-forward 分数对 y：IC **0.016**，R² **−0.073**，命中 52.2%。分数 std 0.32%，42,448 个不同值，不是常数。

**H1_NATIVE**：折均训练 IC 0.579 / 测试 IC 0.022；全程 OOS IC −0.025。同一病理。

自动门槛原先把「测试 IC 0.038」标成 `WEAK_FIT_WEAK_OOS`（测试阈值写死 0.02）。人读数字：**过拟合**。训练 IC 高、测试 IC 约 0。不是实现 bug，不是死特征。

## 3. 状态（事前写死，不挑赢家）

H1_ML 净 TWR。买持有 = 该窗第一根收到最后一根收。

| 窗 | 笔数 | ML 净 TWR | 命中 | t | 买持有 | ML − 买持有 |
|----|------|-----------|------|---|--------|-------------|
| COVID 2020-02–06 | 97 | **−13.1%** | 49% | −1.05 | +12.2% | −25pp |
| 加息 2022 | 236 | **−22.7%** | 43% | −1.74 | −0.3% | −22pp |
| 震荡 2023 | 238 | **−12.7%** | 45% | −0.95 | +13.1% | −26pp |
| 金牛 2024-05→2026-09 | 560 | **+29.1%** | 52% | 0.90 | **+90.2%** | **−61pp** |

按年：2019 −2.8%（金 +18%）；2020 −14%（金 +25%）；2021 −14%（金 −4%）；2022 −23%；2023 −13%；2024 +9.8%（金 +27%）；2025 +25%（金 +65%）；2026 −1.8%。  
只有 2024–25 账本正，而且都大幅落后买持有。t 从未过 1.3。禁止把金牛窗写成策略。

V5 金牛窗净 **−14%**（买持有 +90%）。小时钟没有修好过拟合。

## 4. 下一刀从诊断来（不是再堆树）

- 不是常数分 → 不修 bug。
- 不是训练 IC≈0 → 不换 24h 符号问题、不加美元。
- 是训练 IC 高 / 测试 IC≈0 → **同一问句、更简单的模型，只跑一次。**

合同 `HOT_MT5_GOLD_H1_V6_RIDGE`：同一十四列、同一 24h 标签、同一 walk-forward、`sign(score)` 持有 24 根。模型改成 **标准化 + Ridge(α=1)**。不搜 α、不搜叶、不搜持有。

## 5. V6 一次读取（同一天）

真样本内 IC **0.055**、R² **0.003**、命中 52.7%。折均训练 IC 0.103 / 测试 IC 0.007。净 TWR −52%，验证 +1.0% t 0.20。线性几乎拟合不动；树的样本内 IC 0.52 是过拟合。见 `HOT_MT5_GOLD_H1_V6_DECISION.md`。
