# HOT_MT5 长任务（用户 2026-09-13 13:06）

> 用户：持续分析订单 / 每笔成交 / 模型输出 / 策略，找薄弱点，持续优化；不要等一句做一句。
> **优化 ≠ 看完账本改参数。** 每发现一个结构弱点，开一份新合同、只算一次、冻结，立刻下一刀。
> 不改 V1/V2、不加大 λ、不用 20bp、不写 `:9000`、不与 A 股对冲、不 `order_send`。

## 已看见的结构弱点（不是调参借口）

| 弱点 | 证据 | 下一刀 | 禁止 |
|------|------|--------|------|
| 策略永远在场 | V1 `sign(score)`；V2 覆盖 82–91% | V3 用波动门槛，不是 2×点差 | 加大 V2 的 λ |
| 门槛相对波动太矮 | V2 CASH 标签 4–20%，门槛 10–33bp | V3：`ATR14/close × √hold`（1σ 布朗，教科书） | 用法医 20bp |
| 模型无边 | V1 IC ≈ 0；V2 命中 42–49%、盈亏比 ≈1 | V3 仍用同一特征（只改标签）；若再失败则 **停 own-price ML** | 加 RSI 变体 / 搜 H1 |
| 黄金开头常数空 | V1/V2 都从 2019-12 连空顶着上涨 | 诊断；不为此改 hold | 为 2020 切片改模型 |
| 用户要的是低命中高盈亏比 | V1/V2 都是 50% × 1.0 | V4：12 月 TSMOM，无 ML | 换一条均线重开黄金 |

## 队列（事先写死，按序各一次）

1. **V2 法医** — 只读成交，不重训。
2. **`HOT_MT5_ATR_BARRIER_V3`** — 三分类，门槛 = 当日 ATR14/收盘 × √hold。其余同 V2。
3. **`HOT_MT5_TSMOM12_V4`** — 无树。信号 = sign(收盘 / 252 日前收盘 − 1)；下一开盘、持有 20 根、非重叠、同一成本。ATLAS 里 FX3 是 CME 30 只截面，不是这七个 Ava 单品种账本；仍禁止改成 SMA200。
4. 若 V3+V4 都否 → `OWN_PRICE_D1_SEVEN_EXHAUSTED`，停 own-price D1。不下 H1、不买数据、不加美股。

状态机：`docs/research_engine/HOT_MT5_EVOLUTION_STATE.json`。
