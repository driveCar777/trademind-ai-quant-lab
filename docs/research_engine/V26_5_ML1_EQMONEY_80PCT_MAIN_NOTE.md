# V26.5 备注 — 总仓 70% → 80%（`ML1_EQMONEY_80PCT_MAIN`），**不重读历史**

> 2026-09-06 00:25 用户："资金利用率允许达到 80%，必要时允许达到 100%"。

## 决定

- 前向合同改为 V26.4 的全部规则 + 总仓 **80%**（N = floor(0.8 × 权益 / 2000) = 8 起）。`daily.py` 默认 `--exposure 0.80`。
- **不做 80% 的历史读取。** 理由：仓位比例对结果的影响是算术性的（投入部分同一套名单，只是多一只、少 10% 现金），再读一次历史就是用户 23:36 自己禁止的"在历史上试仓位比例"。V26.4 的 70% 读取作为本合同的历史依据，标签沿用 `VIABLE_HISTORICAL（继承 V26.4）`。
- 事前预期：验证期收益约为 V26.4 的 8/7 倍量级（+13.4% → ≈ +15%），回撤同比例放大（−7.9% → ≈ −9%），现金闲置从 ≈49% 降到 ≈42%（按手取整零头仍在）。这是预期，不是读数。
- **100%**：系统不会自动切到 100%。"必要时"是人的判断，用户手工加仓即可；影子账本按 80% 记，以便前向数字与合同一致。若用户日后决定系统规则改为 100%，那是新合同，同样不重读历史。

## 禁止

不为 80% / 100% 重跑研究期或验证期；不读禁用窗；不调 ¥2,000 单位、N 公式、顺延天数。
