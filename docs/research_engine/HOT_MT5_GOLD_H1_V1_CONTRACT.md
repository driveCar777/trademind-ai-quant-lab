# HOT_MT5_GOLD_H1_V1 合同（跑前冻结）

> 2026-09-13。用户纠正：计划是黄金**小时 / 15 / 30 分钟**交易，不是日线月线。
> 此前 V1–V5 全是 D1。H1 只存档。这是第一份 H1 合同。只用已在盘上的 `GOLD_H1.csv`（2018-12→2026-09，约 4.6 万根）。
> **不做** 15/30 分钟（还没拉，另开合同）。**不用**最近一个月挑持有期。不覆盖 D1 `READ.json`。`candidate=false`。

## 两份假设（m=2，同一批 H1）

特征（ML）：与 D1 同一套价内特征，另加 `HOUR`（0–23）。SMA200 在 H1 上是约 8 个交易日，不是 200 个自然日。

1. **H1_ML**：LightGBM 回归，标签 = 下一根开盘起持有 **24** 根小时线的价格变动。策略 = `sign(score)` 永远在场（V1 外壳搬到小时）。树参数与 D1 V1 相同。Walk-forward：首预测 2000 根，每 1000 根重拟合，embargo=25。
2. **H1_TSMOM**：无树。`sign(close[t]/close[t-120]−1)`（120 小时 ≈ 一周，不是 12 个月）。持有 24 根。

成交 = 下一根小时开盘，非重叠。成本 = 当根点差 + 2×2bp + **跨过的自然日** swap（周五三重；同一小时交易日内进出不收隔夜）。不是把每根 H1 当成一夜。

## 闸门

主报告 = 全样本。验证 = 后 30%。验证成交≥8 且覆盖≥15% 且 TWR>0 且 t>1 → `VIABLE_HISTORICAL`。`candidate` 仍 false。最近一个月只诊断。

## 禁止

搜 8/12/36 小时持有；改 120 成 252×24；用 8 月回撤选参；拉 15/30 当本份；写 Grok；A 股对冲。
