# V26.6 合同 — `ML1_EQMONEY_80PCT_TOPUP_MAIN`（补满仓位到目标 80%）

> 2026-09-06 09:15，跑前冻结，只算一次。用户 09:06："根据你的来，继续优化，效果差"。

## 为什么是这一刀（事前理由，不看收益）

V26.5 读数里有一个**算术事实**：验证期平均现金闲置 **42.4%**，目标是 20%。原因是 ¥2,000/只 + 整手：¥15 的股票买 1 手 ¥1,500 剩 ¥500；¥20 以上的股票 1 手都买不起被跳过；8 个名额里每个都剩一截。结果 ¥20k 里只有 ≈¥11.5k 在市场里。这不是 alpha 参数、不是选股、不是出场——是钱没投出去。V26.5 验证 +12.1% 是 58% 仓位做出来的。

## 规则（相对 V26.5 只改一处）

- 第一轮：同 V26.5（主板、收盘 ≤¥100、N = floor(0.8×权益/2000)、每只 lots = floor(2000/(100×开盘))、买不起 1 手跳过）。
- **第二轮（新增）**：预算 = 0.8×权益 − 第一轮已用。按分数从高到低在**已选名单内**循环，每次给一只加 1 手，只要加得起；直到预算不够任何一只再加 1 手。不增加名字（不多付 ¥5 最低佣金），不改选股。
- 其余全部同 V26.5：出场 20 日后开盘、顺延 ≤10 日、成本模型 V1 + ¥5 最低佣金、¥20k 起。

## 门与采纳规则（事前）

- 研究 2012-01→2021-08、验证 2021-08→2024-02，不读禁用窗、不读近期窗。
- `VIABLE_HISTORICAL`：验证资金 > 0 且超额 vs 同口径 EW > 0。
- **采纳为 `daily.py` 默认**当且仅当：VIABLE 且验证总收益 > V26.5 的 +12.1%。否则 V26.5 不变。这是唯一一次比较，不再有 V26.7 去试别的补仓方式。

## 事前预期

仓位从 ≈58% 提到 ≈78%，收益与回撤大致按 1.3× 放大：验证 +12% → 约 +16%，MaxDD −11% → 约 −14%。不改变超额 t 的量级。**这不是变成 10% 以上 CAGR 的路径**，是把用户已经同意的 80% 真正做到。

## 禁止

不试其它补仓顺序/单位/仓位；不因结果回头改 V26.5；不改 ML1。
