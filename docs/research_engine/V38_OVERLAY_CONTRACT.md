# V38-S2 合同 — 市场级仓位规则叠加在 V26.8 账本上（跑前冻结）

> 2026-09-06 23:45 提交。`research_engine/v38_overlay/run.py`。用户 22:45 允许"外壳级市场仓位规则"作为预注册合同各测一次。ML1、名单、外壳其余全部不动。
> `V38_OVERLAY_READ.json` 存在即拒绝再跑。

## 1. 对象

V26.8 已读账本（`ML1_SCALED_UNIT_FULL_CONTRIB2K_MAIN_READ.json` 的 `research_trades` / `validation_trades` + 逐日 TWR 曲线 `V26_8_DAILY_CURVE_*.npy`）。规则只改**每期的仓位比例** e_k ∈ {1.0, 0.5}，在信号日收盘决定，作用于该期从入场到下一期入场的全部逐日收益：r'_d = e_k × r_d（另一半现金收益 0）。

近似声明：真实执行时 ¥20k × 50% 会使 N 减半并带来整手取整差异；本合同以"账本收益按比例缩放"近似，事前接受。

## 2. 三条规则（各一次；O3 待 L8 情绪指数建好后再跑，本次只跑 O1、O2）

| ID | 信号（信号日收盘可知） | 规则 |
|---|---|---|
| O1_TREND | 主板合格股票等权指数（与外壳同宇宙：主板、非 ST、收盘 ≤¥100）收盘 vs 其 200 日简单均线 | 指数 < SMA200 → e=0.5，否则 1.0 |
| O2_VOL | 账本自身逐日 TWR 收益的 20 日标准差 | > 研究期该序列中位数 × 1.5 → e=0.5，否则 1.0（阈值只用研究期定） |
| O3_SENTIMENT | 市场级散户情绪指数（L8） | > 研究期 90 分位 → e=0.5（待） |

## 3. 判决（事前）

对每条规则分别：**验证期逐日 MaxDD 改善（更浅）且 验证期日度夏普 ≥ 现状** 两者同时成立 → `ADOPT_CANDIDATE`（再走五阶段进 `daily.py`）；只满足一个或都不满足 → `REJECT`。研究期结果同时报告但不作门。不试别的均线长度 / 阈值 / 仓位比例。m 各 +1（V38 统一 FDR 中记为外壳规则，不参与 alpha FDR）。

## 4. 禁止

不读禁用窗；不改名单；不试 0.3 / 0.7 / 0；不把 O1、O2 组合成第三条（那是新合同）。
