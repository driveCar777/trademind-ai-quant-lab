# V38-L7 合同 — 股权质押层（写于跑之前，2026-09-07 00:55）

> 代码 `research_engine/cn_a_share_pledge_v38/`。合同 JSON 由 `run.py` 首次运行时写入 `CONTRACT.json` 并哈希。**只算一次。**

## 数据

- 东财 `RPT_CSDC_LIST`（中登每周股权质押，`TRADE_DATE` = 周五），免费；2014-03-07 起；每周 ≈3,000 只有质押的名字。
- 字段：`PLEDGE_RATIO`（质押比例 %）、`REPURCHASE_BALANCE`（质押股数）、`PLEDGE_DEAL_NUM`（笔数）、`PLEDGE_MARKET_CAP`（质押市值 万元）。
- **PIT**：周五快照在其后第 1 个交易日 +2 个交易日可见（中登下周初公布，保守）；沿用至下一快照，最多 30 个交易日。
- 面板里有、当周快照里没有的名字 = **零质押**（表只列有质押的），事前声明。
- `TRADE_DATE > 2024-02-29` 全丢（禁用窗）。
- 层"活"的会话 = ≥50% 合格名字有状态；之前的会话不训练不打分。首个样本外预测 = 层起点 + 500 个交易日（V25 "≥2 年标签"规则套到本层；首次运行因 2012 起点无训练行报错后按此修正，**修正前未产生任何结果**）→ **研究期实际打分 ≈2016-03 → 2021-08**，事前声明。

## 六个固定特征（多头方向 = 低质押 / 无压力）

| 特征 | 定义 | 事前方向 |
|---|---|---|
| PL_NEG_RATIO | −质押比例 | + |
| PL_NEG_D20 | −(比例_t − 比例_{t−20}) | + |
| PL_NEG_D60 | −(比例_t − 比例_{t−60}) | + |
| PL_NEG_DEALS | −log1p(笔数) | + |
| PL_NEG_MCAP | −质押市值 / 20 日均成交额 | + |
| PL_STRESS | 比例 × min(ret60, 0) / 100 | + |

文献（跑前记录）：高/上升质押率预示崩盘风险与跑输（2018 平仓潮；谢德仁 2016）。

## 模型 / 闸门 / 判决

与 V25/V27/L1/L4 完全一致：LightGBM 同参数，walk-forward，2021-08 后冻结；LO20 闸门 + HN20 只报；固定划分 + 5 滚动窗 ≥4/5；FDR m=1；A4 超额序列 corr vs ML1 > 0.90 = 同簇。

标签：`A_SHARE_EQUITY_PLEDGE_V38L7_{INDEPENDENT_CANDIDATE | SAME_CLUSTER | NO_CANDIDATE}`。

## 禁止

不搜特征/滞后/参数；不在看到结果后当 ML1 过滤器用；不换账本；不读禁用窗。
