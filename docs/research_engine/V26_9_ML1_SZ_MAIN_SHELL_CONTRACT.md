# V26.9 合同 — `ML1_SCALED_UNIT_N10_FULL_CONTRIB2K_SZMAIN`（只买深市主板）

> 2026-09-07 22:10，跑前冻结。分支 `exp/sz-main-shell`，独立目录 `D:\AGXXAIVER-4-WINDOWS-1-STOCK-SZ`，Master 端口 9001。主线 `main`（冻结标签 `freeze-20260907-paper-ops-v2.1`）不动。
> 用户 21:59 提问："测一下纯深指的股"。理解为**深市主板**（`sz.00`，000/001 开头）。深证成指成分股需要成分历史，V20 只有沪深 300 / 中证 500 → 那种读法 DATA_BLOCKED，不做。

## 这次读什么

与 V26.8 完全相同的外壳，只改板块过滤一处：

| 项 | V26.8（当前 `daily.py` 默认） | V26.9（本次） |
|---|---|---|
| 板块 | MAIN = `sh.60` + `sz.00` | **SZ_MAIN = `sz.00`** |
| N_target | 10（V26.8 研究期已选，不重选） | 10 |
| 单位 | max(¥2,000, 权益 ÷ 10) | 同 |
| 仓位 / 预留 / 定投 / 价格上限 / 持有期 / 成本 / 顺延 | 100% / ¥200 / 每月 ¥2,000 / ≤¥100 / 20 日 / V1 + ¥5 / 10 日 | 全同 |
| ML1 分数 | `SCORES_ML1_LGBM.npy`（冻结） | 同一文件，一个字不动 |

对照 EW = 同一板块过滤下的合格股票等权（不是全市场 EW）。

## 读取窗口

研究 2012-01-04→2021-08-24、验证 2021-08-25→2024-02-29，**各读一次**。禁用窗 2024-03→2026-08 与近期窗**不读**。

## 事前判定

- `VIABLE_HISTORICAL`：验证期 TWR > 0 **且** 超额 vs 同板块 EW > 0。
- 否则 `NOT_VIABLE`。
- 与 V26.8 MAIN 并排报告（研究 / 验证：TWR、CAGR、逐日 MaxDD、单期夏普、超额 t、平均只数、IRR）。

## 事前声明（结果怎么用）

- 板块过滤**由账户权限决定，不由结果挑**（用户 2026-09-05 22:20 约束）。用户账户能买沪深两市主板，所以 V26.9 即便更好，也不自动替换 V26.8；替换是用户的决定，且要记作"按结果选板块"（计 m）。
- 本次读取的主要用途：回答"只做深市会怎样"，并给 9001 分支一条独立影子名单做前向对照。
- 不再读 SH_MAIN 单独一条（那是第二次读；如用户要，另立合同）。

## 禁止

不动 ML1；不换 N_target / 单位 / 仓位 / 价格上限；不读禁用窗；不因为结果回头改主线 `daily.py`。
