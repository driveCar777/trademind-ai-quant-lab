# V26.5 合同 — ML1 手工执行版，等金额 + 总仓 80%（`ML1_EQMONEY_80PCT_MAIN`）

> 2026-09-06 00:32，跑前冻结，**只算一次**。用户 00:30："资金利用率允许达到 80%，必要时允许 100%，允许复利抛掉后重新投入"，并要求看最近一年。除总仓比例外与 V26.4 逐字相同。新合同（m+1）；ML1 分数不动。

## 1. 与 V26.4 的差别（唯一）

| 项 | V26.4 | V26.5 |
|---|---|---|
| 总仓 | 70% | **80%** |

其余全部相同：主板、收盘 ≤ ¥100、每只 ¥2,000、N = floor(0.80 × 权益 / 2000)（¥20k 起 = 8）、`lots = floor(2000/(100×开盘))`、20 日持有、t+1 开盘买、t+21 开盘卖、出场遇跌停/停牌顺延最多 10 日、成本模型 V1 + ¥5 最低佣金。

**"必要时 100%"不采纳**：什么叫"必要"需要一条事前触发规则；任何从结果里找出来的触发规则都是调参。用户若给出一条与结果无关的规则（例如"每月追加资金到账当天"），再登记。

**"复利抛掉后重新投入"**：所有账本一直如此——每期卖出后的全部权益进入下一期的 N 计算，N 随权益增长。

## 2. 读取范围

- **门**：研究 2012-01→2021-08、验证 2021-08→2024-02，冻结分数。门 = 验证期资金收益 > 0 且 − 合格 EW > 0。
- **近期诊断（无决策）**：2024-03-01→2026-08-28，用 V28 已经读过一次的 REFIT_240 闸门分数，对本合同（只有本合同，不对 V26.4，不做 70/80 对比）报一次数字：全窗、近 12 月、近 6 月，附同口径（主板 ≤¥100 合格股）EW。**该窗口在 V28 已消耗**，这里的数字不能作为验证，只能告诉用户"这台机器最近一年在这个外壳里长什么样"。**禁止**据此挑仓位 / N / 板块 / 价格。

## 3. 事前预期

- 80% vs 70%：收益与回撤都放大约 1/7；验证期应仍为正；现金闲置从 ≈49% 降到 ≈40%。
- 近期窗：V26.2 诊断显示 2024-03 以来低价主板切片的超额 ≈ 0（纯 β），近 6 月 −12.6%。本合同（¥2,000 等金额，主板 ≤¥100）与 V26.2 结构相近，预期类似：全窗为正、近 6 月为负、超额不显著。

## 4. 禁止

跑完不调任何参数；不做 70/80/90/100 对比；不用近期窗做门；不自动下单。
