# V26.4 合同 — ML1 手工执行版，等金额多手 + 固定总仓 + 真实出场（`ML1_EQMONEY_70PCT_MAIN`）

> 2026-09-06 00:15，跑前冻结，**只算一次**。用户 00:13 改约束："允许每只按等金额买多手，像 V26.2 那样；你也可以改"，并要求回测尊重"当天收盘后才有数据、最早次日开盘卖、次日跌停卖不出"。V26.3 拆解已证明一手制（权重 ∝ 股价）是验证期 −10.8% 的主因（同名单等权毛收益 +2.27% vs 一手制 +0.50%）。本合同是新合同（m+1）；ML1 分数不动；不是为了"把数字做好看"，而是把执行外壳换成用户现在允许的形态，并把出场规则改得更严。

## 1. 定义（事前固定）

| 项 | 值 | 来源 / 理由 |
|---|---|---|
| 分数 | ML1 原样 | 不改模型 |
| 板块 | 仅主板 | 账户权限 |
| 价格上限 | 信号日收盘 ≤ ¥100 | 用户；实际由每只金额决定能买什么 |
| 总仓 | **70%** 权益可投，其余现金 | 用户示例数字，沿用 V26.3，不试其他 |
| 每只金额单位 | **¥2,000** | 算术：¥5 最低佣金 ≤ 0.25%/边的最小金额；沿用 V26.2 单位，不搜 |
| N | `floor(0.70 × 权益 / 2000)`，¥20k 起 = **7**；随权益变化，不回头选 | 算术 |
| 手数 | `lots = floor(2000 / (100 × 次日开盘))`；0 手跳过顺延 | 普通账户按手 |
| 选法 | 合格股票按分数从高到低，取满 N 只 | |
| 信号时点 | t 日收盘后算分；**t+1 开盘买** | 用户指出的数据可用性；与所有账本一致 |
| 持有 | 20 个交易日；计划 t+21 开盘卖 | |
| 入场不成交 | t+1 开盘涨停/停牌/无量 → 不买，现金留存 | V14.1 规则 |
| **出场不成交（新，更严）** | t+21 开盘跌停/停牌 → **不能卖，继续持有**，逐日尝试次日开盘，最多顺延 10 个交易日；仍卖不出 → 按第 10 日收盘计价标记 `STUCK`。**不再像 V14.1 那样把整笔交易当作没发生** | 用户 00:13 要求；比历史账本严格 |
| 成本 | 成本模型 V1 + 最低佣金 ¥5/笔 | |
| 资金 | 历史读取 ¥20,000 起，不模拟月度追加 | |

## 2. 读取范围与门

- 历史：研究 2012-01→2021-08、验证 2021-08→2024-02，冻结分数。**不读禁用窗。**
- 门：验证期资金收益 > 0 且 验证期 − 合格 EW > 0 → `VIABLE_HISTORICAL`；否则 `NOT_VIABLE`。
- 同一批成交按 V14.1 旧出场规则也算一遍，**只作诊断**（量化"出场卡住"对结果的影响），不作门。
- 前向：V29 `daily.py` 默认改为本合同；`SHORTLIST` 输出 N 只等金额名单；`LEDGER_TOP20.json` 记本合同影子账本（含顺延出场）。

## 3. 事前预期

- 与 V26.2（10 只 × ¥2,000、100% 仓、≤¥20）结构相同，只是 7 只、70% 仓、出场更严。验证期方向应接近 V26.2（+32.7%），数值因 30% 现金和出场顺延而更低；7 只比 10 只更集中。
- 出场顺延会把一部分跌停日的亏损真实计入，结果应低于 V14.1 规则版本。
- 结论不影响 `OWNER_CONSTRAINTS_AND_GOAL.md` 的算术：本金决定数量级。

## 4. 禁止

跑完不调金额单位 / 总仓 / N 公式 / 出场顺延天数 / 成本；不读禁用窗；不自动下单；不用 V14.1 诊断数字替代门。
