# V26.2 附录 — 用户真实约束下的手工版（`ML1_TOP10_10K_MAIN`）

> 2026-09-05 22:45，跑前冻结。用户 22:40 给出事实：**平安账户只能买主板；本金 ¥10,000；偏好 ¥10 左右的股票。**

## 为什么 V26.1 的 N=20 在 ¥10k 下不成立

¥10,000 / 20 = ¥500 一只，而一手（100 股）¥10 的股票要 ¥1,000。按 V26.1 的手数规则，几乎所有名字都会因 `lots = 0` 被跳过。N 必须由资金和最小交易单位**推出来**，不是挑出来：

- 每只至少能买一手 ≈¥10 股 → 每只 ≥ ¥1,000 → **N ≤ 10**
- 最低佣金 ¥5 / 笔在 ¥1,000 上 = 0.5% / 边 = 1.0% 往返，加滑点 0.2% 与印花税 0.05% ≈ **1.25% / 期**；V26.1 验证期超额 vs EW 约 2%/期 → 毛利的一半以上会被费用吃掉。这是事前写下的预期，不是事后解释。

## 定义（固定）

| 项 | 值 |
|---|---|
| 分数 | ML1 原样 |
| 板块 | **仅主板**（sh.60 / sz.00）——用户权限事实 |
| N | **10** |
| 资金 | **¥10,000**（账本复利；历史读取里权益会增长，手数随之变化，这是真实行为） |
| 手数 | `lots = floor(权益/10 ÷ (100 × 次日开盘))`；0 手跳过顺延 → 价格 > 权益/1000 的股票自然进不来（起点即 ≤¥10，与用户偏好一致，不是额外过滤） |
| 成本 | 成本模型 V1 + 最低佣金 ¥5 / 笔 |
| 其它 | 与 V26.1 相同；不读禁用窗 |

## 诊断（非门）

同一次跑 N=5（每只 ¥2,000，费率减半但更集中）只作诊断记录，**不用来选**。主合同是 N=10。

## 门与标签

同 V26.1：验证期资金收益 > 0 且打过合格 EW → `ML1_TOP10_10K_MAIN_VIABLE_HISTORICAL`；否则 `NOT_VIABLE`——含义是 **¥10k 这个规模下 ML1 不可执行**，需等本金变化再登记新合同，不改 ML1。
