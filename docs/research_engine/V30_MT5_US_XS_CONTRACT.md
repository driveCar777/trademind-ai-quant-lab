# V30 合同 — MT5 美股 CFD 截面价格层模型（预注册，跑前冻结）

> 家族 `MT5_US_SHARES_XS_PRICE_V30`。规则依据 `RESEARCH_RULES_AMENDMENT_V2_MT5.md`。$0。结果无论如何都标 `UPPER_BOUND_SURVIVORSHIP`。

## 1. 数据（冻结 `tm-mt5-USSHARES-D1-20260905-000001`）

- 本机 Ava Trade MT5 终端，`CFD-Shares\USA` 全部符号，D1，`copy_rates_from_pos` 最多 6000 根，spread 列为经纪商逐 bar 点差。
- 合约规格快照 `specs.json`：`swap_mode=5`（年化利率 % 按现价），`swap_long ≈ −11.09`，`swap_short ≈ −0.91`，`swap_rollover3days=5`（周五三倍）。
- 时间轴 = 所有符号 D1 时间戳的并集（美股交易日）。

## 2. 合格（每个交易日 t）

有效收盘、`tick_volume > 0`、历史 ≥ 270 根、收盘 ≥ 5 美元、当前 `trade_mode = 4`（完全可交易）。

## 3. 特征（7 个，固定；全部只用 ≤ t 收盘）

`REV_20`（−20 日收益）、`MOM_250_20`（250 日收益去掉最近 20 日）、`NEG_VOL_60`、`NEG_VOL_120`（−日收益标准差）、`NEG_LOG_AMT_20`（−log 20 日均 tick_volume×close）、`VOLU_RATIO_20_120`（20 日均量 / 120 日均量）、`NEG_MAX_RET_20`（−20 日最大单日收益）。截面排名到 [−1,1]，同 V25 `ranked_row`。

## 4. 模型（一个，A3）

LightGBM 回归，参数 = V25 `LGBM_PARAMS` 逐字。目标 = 20 日前向开盘到开盘收益的截面排名（V25 `_label_row`）。训练行 = 2004-01 起每 5 日、标签完全可知（≤ refit − 21）。Walk-forward：首次预测 **2006-01-03**，之后每 **240** 个交易日 refit（= V26 政策），expanding。seed 固定。

> 跑前修订（2026-09-05 17:40，分数尚未生成）：拉取完成后发现 354/492 只历史从 2008 前开始（221 只从 2002），原稿按 2018 起假设写的 2020-12 首预测 / 120 日 refit / 2024 起验证作废，改为与 V25 同构的长划分。修订发生在任何分数产生之前，不构成事后选择。

## 5. 账本与成本（实测）

- 信号 t 收盘 → t+1 开盘进 → t+21 开盘出；等权；不成交（无 bar）留现金。
- **闸门账本 LS20**：多前 20% / 空后 20%，各 100% 资金（美元中性，总敞口 2×）。
- 诊断账本 LO20：多前 20%。
- 成本每边：点差 = 该符号 60 日中位 spread%（bar spread 列 × point / close）的一半；滑点 0.05%；隔夜利息 = `swap_long`/`swap_short` × 持仓日历天数（含周末三倍）/365 × 名义。
- 基准：合格等权 20 日前向（EW）。

## 6. 划分与门

- 研究 2006-01-03 → 2019-12-31；验证 2020-01-02 → 2026-08-28（含 2020 崩盘、2022 熊市）。
- 滚动窗：2008-10 / 2011-13 / 2014-16 / 2017-19 / 2020-26（LS 资金收益 ≥ 4/5 为正）。
- **Level-1（闸门 LS20）**：验证 LS20 资金收益 > 0；验证多空前向价差均值 t ≥ 3；滚动 ≥ 4/5；研究 LS20 > 0。
- FDR：m = 1（本合同）；跨版本累计入 ATLAS。
- 通过 → 标签 `MT5_US_XS_PRICE_V30_LEVEL1_UPPER_BOUND`，下一步 = 无幸存者宇宙复现 + V31 EDGAR 层；**不 Paper**。
- 未通过 → `MT5_US_XS_PRICE_V30_NO_CANDIDATE`，价格层冻结，直接进 V31（信息层是主因的假设仍成立）。

## 7. 禁止

跑完不改特征/参数/hold/分位/成本/refit；不看验证期选变体；不用 LO20 替 LS20 当闸门；不加 ETF/欧股；不做 H1。
