# V28 — ML1 Final OOS 单次读取协议（预声明）

> 写在读取之前。读一次，读完不改任何东西。
> 授权：用户 2026-09-04 21:07「你自己评估一下吧……按照你的想法来」——把禁用窗读取的决定权交给研究流程。评估见 §1。
> 机器：`data/market/research_engine/cn_a_share_ml_v25/FINAL_OOS_PROTOCOL.json`（hash 先写）→ 读取结果 `FINAL_OOS_READ.json`（存在即锁，脚本拒绝二次运行）。

## 1. 为什么现在读（评估）

- 禁用窗 2024-03-01 → 2026-08-28 是 **2.5 年从未被任何模型、任何合同、任何人眼看过的数据**。ML1 的分数在 2021-08-24 冻结，比这段还早 2.5 年。
- ML1 是唯一 Level-1：验证 +1.47%/20d（t 17）、LO20 验证 +30.9%、复现 7/7、placebo 干净、与 H11 超额正交（0.06）。修正案规定的"先过 A1+A2 门"已满足。
- 不读的替代方案是 Paper 12 个月——得到的信息只有这次读取的 1/2.5，还要先搭实时管线。**读取的信息价值最高、成本为零、不改模型。**
- 代价：读完这扇门对 ML1 永远关上。若失败，ML1 降为 KEEP_LOW_PRIORITY，NEW_INDEPENDENT 对可交易目的回到 0。这是真相，不是损失。
- 风险控制：分数冻结、账本冻结、成本冻结、通过标准先写，读后不许调任何参数。

## 2. 读什么

| 项 | 规定 |
|---|---|
| 信号 | ML1_LGBM_STACK，V25 合同的 14 特征、同 LightGBM 参数、同排名变换、同标签 |
| **闸门分数 = V26 运营政策** | 自 2012-01-04 起 expanding walk-forward，**每 240 交易日 refit**（V25.1 已验证的 REFIT_240 变体 = V26 规定的 live 政策），embargo 21，训练行每 5 日，一路走到 2026-08-28。2024-03-01 时的操作者手里有 2024-02 之前的全部数据，这不是前视 |
| 诊断分数（只报告） | V25 官方冻结模型（2021-05-25 最后一次 refit）直接给禁用窗打分：测信号本身冻结 5 年后是否还活着 |
| 账本 | LO20 legacy 多头账本，`A_SHARE_STRATEGY_COST_MODEL_V1` 不变，20 日 hold，前 20% 等权，涨跌停/停牌规则不变 |
| 基准 | 合格 EW（同 overlapping 定义）；预测超额 = MEAN_FORWARD − EW |
| 窗口 | 只读 2024-03-01 → 2026-08-28 的信号日；最后一笔 exit ≤ 2026-08-28 |

## 3. 特征延伸（原始数据，不是研究决定）

| 层 | 现状 | 动作 |
|---|---|---|
| 价格面板 | 到 2026-08-28 | 无 |
| 融资融券（V23） | raw 到 2024-02-29 | 下载 2024-03→2026-08 每日明细（东财，免费，同脚本、同 PIT lag 1）|
| 股东户数（V24） | raw 到 2026-09 | 重编译，公告日截止改到 2026-08-28（PIT 规则不变）|
| 年报（V16） | 到 2026-08 | 无 |
| HS300 成分（V20） | as-of 到 2024-02-15 | BaoStock 月度 as-of 下载 2024-03→2026-08（同脚本）|
| 特征缓存 | `v25_features` | 写到**单独目录** `v25_features_finaloos`，不覆盖 V25 冻结缓存；≤2024-02-29 的值应与冻结缓存逐位相同（脚本断言）|

## 4. 通过标准（先写）

在禁用窗内，闸门分数 + LO20：

- **G1** 预测超额 vs EW 均值 > 0
- **G2** LO20 资金账户成本后总收益 > 0
- **G3** 未触发 V26 退役条件（24 期连续 LO−EW ≤ 0）

| 结果 | 标签 | 含义 |
|---|---|---|
| G1 ∧ G2 ∧ G3 | `FINAL_OOS_PASS` | ML1 → **Paper 准备**成为下一门（需用户 + 行情源 + 实时五源管线）|
| G1 ∧ ¬G2 | `FINAL_OOS_EXCESS_ONLY` | 信号活着、账本没赚钱；ML1 保留 Candidate，不 Paper，不调 |
| ¬G1 | `FINAL_OOS_FAIL` | ML1 → KEEP_LOW_PRIORITY；对可交易目的 NEW_INDEPENDENT=0；不调、不 refit 抢救 |

诊断分数的结果只记录，不改变标签。

## 5. 读后禁止

- 不改特征、参数、hold、成本、分位、refit 节奏。任何改动 = 新合同 = 新家族，且**再无禁用窗**可验。
- 不用禁用窗结果去挑 REFIT_120/240 或任何变体。
- 不再对 ML1 做第二次读取。`FINAL_OOS_READ.json` 存在即拒绝。
- 不 order_send，不 Paper（Paper 是独立的门，需用户）。
