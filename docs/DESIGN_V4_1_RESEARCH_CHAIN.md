# V4.1 两步研究 — Design

> **状态：** ✅ FROZEN（2026-08-23）。  
> **日期：** 2026-08-23

## 1. 要解决什么

V4.0 一笔研究只有 1 次计算。  
V4.1 允许**最多两步**现有预设（例如因子后再回测），仍是人手点，仍不下单。

## 2. 冻结范围

| 做 | 不做 |
|----|------|
| `chain` 长度 1～2 | 3 步、自动扫 50 只 |
| 规则选预设（空 = 在线推荐） | 模型自己发明 Worker 接口 |
| 第一步失败 / 第二台离线 → 不跑下一步 | 队列、重试、MT5 |
| 至多 1 次 AI（最后一步） | 每步各问一次模型 |
| 复用 TM-1001…1005 | 新错误码、新前端框架 |

## 3. 控制台

保留「研究一笔」（单步）。  
增加「因子后再回测」：仅当 02 与 03 都在线可点。  
`init()` 仍禁止自动 `POST /research/run`。

## 4. 冒烟

1. `chain` 3 个名字 → TM-1001。  
2. `chain=["factor","backtest"]` 两台在线 → `steps` 长度为 2，最后摘要含回测字段。  
3. 研究进行中再 POST → TM-1005。  
4. 源码有「因子后再回测」，`init()` 不自动跑。

## 5. 五步

| 步 | 状态 |
|----|------|
| 1 Design | ✅ 本文 + SPEC §17 + Decision 025 |
| 2 Implement | ✅ |
| 3 Smoke | ✅ `tests/smoke/13_research_chain.py` |
| 4 Stability | ✅ `tests/stability/02_research_chain.py` |
| 5 Freeze | ✅ |
