# research_protocol

Research Protocol V0.3 — causal sandbox, experiment governance, leakage sentinel.

Not a backtest engine. Not V11.7. Xavier runs `node_runner.py` as a one-shot probe.

Windows orchestrates via `scripts/research_protocol_run.py`. Results live in `data/market/research_protocol/`.
