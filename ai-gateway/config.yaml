service_name: trademind-ai-gateway
version: "3.0.0"
host: "0.0.0.0"
port: 9100
log_level: INFO

model_path: "models/qwen2.5-14b-instruct-q4_k_m.gguf"
model_name: "Qwen2.5-14B-Instruct"
n_ctx: 4096
n_gpu_layers: -1

# Timeout for single inference request (seconds)
request_timeout_seconds: 120
